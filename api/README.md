# API Directory

This folder documents API grouping for hackathon submission.

Implemented API routes live in `app/api`:

- `POST /api/ai` - AI assistant mock response
- `POST /api/payments/mpesa` - M-Pesa STK Push simulation
- `POST /api/sos` - Emergency alert simulation
